
import React from "react";

// Placeholder for custom edge rendering in the future.
// Currently, edges are handled natively by ReactFlow.

const GraphEdgeRenderer = () => null;

export default GraphEdgeRenderer;
